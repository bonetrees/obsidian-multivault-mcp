# API keys for each Obsidian vault, one env var per vault.
# The variable name must match the `api_key_env` field in the YAML config.
# Generate keys in Obsidian → Settings → Local REST API.

OBSIDIAN_DEVPROJECTS_API_KEY=
OBSIDIAN_PERSONAL_API_KEY=
